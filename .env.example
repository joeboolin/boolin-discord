# Discord
DISCORD_BOT_TOKEN=
DISCORD_CLIENT_ID=       # Bot's application ID — needed to register slash commands
DISCORD_GUILD_ID=        # Your server ID — for guild-scoped command registration (instant)

# Channel IDs — right-click a channel in Discord → Copy Channel ID
DISCORD_LIVE_CHANNEL_ID=    # #live-content-chat
DISCORD_CONTENT_CHANNEL_ID= # #content-chat

# Supabase — same project as internal site
SUPABASE_URL=https://mthldobpurkdhyfuskio.supabase.co
SUPABASE_SERVICE_ROLE_KEY=  # Settings → API → service_role secret key (NOT the anon key)
